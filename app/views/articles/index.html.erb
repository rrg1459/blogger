<h1>All Articles</h1>

<p><%= link_to 'See all tags', tags_path if @count > 0 %></p><br>

<ul id="articles">
	<% @articles.each do |article| %>
		<li type="circle">
			<%= link_to article.title, article_path(article) %>
		</li>
	<% end %>
</ul>

<%= link_to "Create a New Article", new_article_path, class: "new_article" %>

<!--